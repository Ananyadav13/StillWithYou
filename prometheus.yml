global:
  scrape_interval: 15s

scrape_configs:
  - job_name: stillwithyou-api
    static_configs:
      # The API runs on the host, not in compose, so reach it via the Docker
      # Desktop host alias rather than a service name.
      - targets: ["host.docker.internal:8000"]
